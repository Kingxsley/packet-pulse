pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
scipy==1.17.1
matplotlib==3.11.1
seaborn==0.13.2
plotly==6.9.0
shap==0.51.0
joblib==1.5.3
psycopg2-binary==2.9.10
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
python-multipart==0.0.32
requests==2.34.2
itsdangerous==2.2.0

# Optional: only needed if you want to restore the original live InfluxDB
# Cloud data source instead of the local packet captures in data/raw/.
# influxdb-client

# Optional: only needed to run the retired Streamlit dashboard in
# legacy/streamlit_dashboard/ (superseded by the FastAPI app in backend/).
# streamlit==1.62.0

# Optional: only needed for `run.py --with-autoencoder`, which the deployed
# app never uses (see README "What we removed"). TensorFlow is a ~1GB
# dependency that used to make every build take 5+ minutes.
# tensorflow==2.21.0
